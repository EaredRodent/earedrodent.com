// import MobileDetect from 'mobile-detect'

export default async function ({ store: { commit }, req, route, redirect }) {
  // if (req) {
  //   if ((new MobileDetect(req.headers['user-agent'])).mobile()) {
  //     commit('other/device/setMobileDetected', true)
  //   }
  //   console.log()
  // }
}
